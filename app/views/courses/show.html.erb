<br />

<div class="booyah-box col-10 offset-1">
  <h1><%= @course.title %></h1>
  <%= image_tag @course.image.url, class: 'img-fluid' unless @course.image.nil? %>
  <br />

  <div class="row">
    <div class="booyah-box">
  <div class="col-sm-4 col-12 text-center">
    Cost:
    <span class="badge badge-success"><%= number_to_currency @course.cost %></span>
    Instructor: <%= @course.user %>
  </div>
</div>
  <%if current_user && current_user == @course.user %>
    <br />
    <%= link_to 'Administer', instructor_course_path(@course), class: "btn btn-danger btn-large" %>
    <br />
    <br />
  <% end %>

  <div class="col-sm-8 col-12">
    <%= @course.description %>
  </div>

  <div class="col-12">
    <% @course.sections.each do |section| %>
    <div class="section">
      <%= section.title %>
      <% section.lessons.each do |lesson| %>
      <div class="lesson">
        <%= link_to lesson_path(lesson) do %>
        <b><%= lesson.title %></b>
        -
        <%= lesson.subtitle %>
        <% end %>
      </div>
      <% end %>
    </div>
    <% end %>

</div>
</div>